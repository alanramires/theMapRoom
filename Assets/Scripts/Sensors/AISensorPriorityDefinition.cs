/// <summary>
/// Sensor/action priorities used by unit data and AI evaluation code.
/// The legacy orchestrator that consumed these priorities lives under AI_Legacy~.
/// </summary>
public enum AISensorPriority
{
    Attack,
    Capture,
    Reposition,
}
